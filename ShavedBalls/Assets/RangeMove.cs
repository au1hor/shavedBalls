using UnityEngine;

public class RangeMove : MonoBehaviour
{
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        rotation = Quaternion.Euler();
        
    }
}
